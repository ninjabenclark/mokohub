import React from "react";
import ReactDOM from "react-dom/client";
import MokoHubHead from "./MokoHubHead.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MokoHubHead />
  </React.StrictMode>
);
